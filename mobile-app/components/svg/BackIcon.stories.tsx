import type { Meta, StoryObj } from "@storybook/react";
import BackIcon from "./BackIcon";

const meta: Meta<typeof BackIcon> = {
  title: "Icons/BackIcon",
  component: BackIcon,
};

export default meta;

export const BackIconSample: StoryObj<
  typeof BackIcon
> = {};
